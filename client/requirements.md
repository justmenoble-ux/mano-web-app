## Packages
recharts | For dashboard analytics charts
framer-motion | For smooth page transitions and micro-interactions
date-fns | For date formatting
clsx | For conditional class names
tailwind-merge | For merging tailwind classes safely
react-dropzone | For drag and drop file uploads

## Notes
Authentication uses Replit Auth (already integrated)
File uploads use FormData to /api/statements/upload
Glassmorphism requires backdrop-blur support in CSS
Dashboard uses Bento Grid layout
